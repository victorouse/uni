$(document).ready(function() {
    /*
    $(".vote").click(function() {
        var self = $(this);
        var action = self.data("action");

        var votebox = self.closest(".votebox");
        var votes = votebox.find(".votes");

        var score = Number(votes.text());

        var btnUp = votebox.find(".up");
        var btnDown = votebox.find(".down");

        $.ajaxSetup({
            url: "inc/api.php?ap=vote",
            type: "POST",
            cache: "false"
        )};

        if (action == "up") {
        } else if (action == "down") {
        }
    });
    */
});
